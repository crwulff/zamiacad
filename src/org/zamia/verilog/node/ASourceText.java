/* This file was generated by SableCC (http://www.sablecc.org/). */

package org.zamia.verilog.node;

import java.util.*;
import org.zamia.verilog.analysis.*;

@SuppressWarnings("nls")
public final class ASourceText extends PSourceText
{
    private final LinkedList<PDescription> _description_ = new LinkedList<PDescription>();

    public ASourceText()
    {
        // Constructor
    }

    public ASourceText(
        @SuppressWarnings("hiding") List<PDescription> _description_)
    {
        // Constructor
        setDescription(_description_);

    }

    @Override
    public Object clone()
    {
        return new ASourceText(
            cloneList(this._description_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseASourceText(this);
    }

    public LinkedList<PDescription> getDescription()
    {
        return this._description_;
    }

    public void setDescription(List<PDescription> list)
    {
        this._description_.clear();
        this._description_.addAll(list);
        for(PDescription e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._description_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._description_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PDescription> i = this._description_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PDescription) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
